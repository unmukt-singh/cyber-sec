{
  "name": "playback",
  "version": "1.0.0",
  "description": "Application to handle playback",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Unmukt Singh",
  "license": "ISC",
  "devDependencies": {
    "electron": "^27.0.2"
  }
}
